terraform {
  required_version = ">= 1.5.0"
  # Declare the megaport provider here once you've picked a version (see
  # ../../modules/megaport-fabric/README.md). Left undeclared so this option
  # remains plan-clean as scaffolding.
}
